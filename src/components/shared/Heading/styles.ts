import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 24,
  },
  title: {
    fontSize: 48,
    textAlign: 'center',
  },
  subtitle: {
    fontWeight: '300',
    fontSize: 18,
    textAlign: 'center',
  },
});
