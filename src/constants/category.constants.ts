import { ISelectOption } from '../types/common';

export const DEFAULT_CATEGORY_NAME = 'Other';

export const DEFAULT_CATEGORY_OPTION: ISelectOption = {
  label: DEFAULT_CATEGORY_NAME,
  value: null,
};
